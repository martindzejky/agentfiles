# Create GitHub PR

Open a GitHub pull request for the current branch. Follow the `github-pull-requests` skill — preflight, title and description conventions, then `gh pr create`.

Return the PR URL when done.
